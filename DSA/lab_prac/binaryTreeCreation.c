#include<stdio.h>
#include<stdlib.h>

struct node{
    int data;
    struct node* left;
    struct node* right;
};

struct node* create(){
    struct node* newnode=malloc(sizeof(struct node));
    int data;
    
}

void main(){
    struct node* root = NULL;

}